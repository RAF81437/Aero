#include <stdio.h>

int main() {
    // Déclarer et initialiser deux valeurs
    int valeur1 = 10;
    int valeur2 = 20;
    
    // Comparer les deux valeurs
    if (valeur1 > valeur2) {
        printf("La plus grande valeur est : %d\n", valeur1);
    } else {
        printf("La plus grande valeur est : %d\n", valeur2);
    }

    return 0;
}
